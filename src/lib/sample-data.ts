// Demo mode data — intentionally empty so prospect demos show a clean, blank app
// (no real client, lead, referrer or task data is ever exposed).

export const SAMPLE_LEADS: any[] = [];

export const SAMPLE_NOTES: Record<
  string,
  Array<{ id: string; content: string; notify_partner: boolean; created_at: string; author_id: string | null }>
> = {};

export const SAMPLE_COMPANIES: any[] = [];

export const SAMPLE_REFERRERS: any[] = [];

export const SAMPLE_LEADS_WITH_REFERRERS: any[] = [];

export const SAMPLE_TASKS: any[] = [];
